name: Trigger auto deployment for streamlit-container-app

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/streamlit-container-app-AutoDeployTrigger-75b08eef-68a3-4e3d-b742-b9ab4b8317d1.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.STREAMLITCONTAINERAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.STREAMLITCONTAINERAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.STREAMLITCONTAINERAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/backend
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: docker12registry.azurecr.io
          registryUsername: ${{ secrets.STREAMLITCONTAINERAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.STREAMLITCONTAINERAPP_REGISTRY_PASSWORD }}
          containerAppName: streamlit-container-app
          resourceGroup: docker-resource-group
          imageToBuild: docker12registry.azurecr.io/streamlit-container-app:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


